import React, { useState, useEffect } from 'react';

const Cart = () => {

  useEffect(() => {
    window.scrollTo({top:0, left:0, behavior:'instant'})
  },[])
  
  return (
    <div>Cart</div>
  )
}

export default Cart